// Import Section
import express from "express";
import dotenv from "dotenv";

// Configuration Section
const app = express();

// Middleware Section
dotenv.config();

// Export Section
export default app;
