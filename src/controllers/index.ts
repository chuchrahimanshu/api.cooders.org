import {
  changePassword,
  generateEmailVerificationToken,
  generateForgetPasswordToken,
  generateTFAToken,
  signin,
  signout,
  signup,
  verifyEmailVerificationToken,
  verifyForgetPasswordToken,
  verifyTFAToken,
} from "./api/v1/accounts.controller";

export {
  changePassword,
  generateEmailVerificationToken,
  generateForgetPasswordToken,
  generateTFAToken,
  signin,
  signout,
  signup,
  verifyEmailVerificationToken,
  verifyForgetPasswordToken,
  verifyTFAToken,
};
